
<%= link_to 'Signin with Google', user_google_oauth2_omniauth_authorize_path(:google) %>
<%= ENV['GOOGLE_APP_ID'] %>
<%= ENV['GOOGLE_APP_SECRET'] %>

<h1><%= @contest.title %></h1>

<% if Time.now < @contest.start_time %>
時間の編集可能 <%= @contest.start_time %>
<% end %>


<% if @contest.end_time < Time.now or Time.now < @contest.start_time %>
  <h2>投票不可能<br>結果の表示</h2>
<div class="row">
  <% @products.each do |pro| %>
    <div class="col s4 m3 result-card">
      <div class="card">
        <div class="card-image">
          <% if pro.image == nil %>
            <%= image_tag asset_path('noimages.jpeg') %>
          <% else %>
            <%= image_tag asset_path(pro.image) %>
          <% end %>
          <span class="card-title"><%= pro.title %></span>
        </div>
        <div class="card-content">
          <p><%= pro.detail %></p>
        </div>
        <div class="card-action">
          <a href="<%= pro.link %>"><%= pro.link %></a>
        </div>
      </div>
      <!-- <% if @contest.end_time > Time.now %> -->
        <div class="result">
          43
        </div>
      <!-- <% else %> -->
        <div class="result">
          ??
        </div>
      <!-- <% end %> -->
    </div>

  <% end %>
</div>
<% else %>
<h2>投票可能</h2>
<%#jsで3つ選んだ感じにできるUI%>
<div class="row">
  <% @products.each do |pro| %>
    <div class="col s4 m3 result-card"> 
      <div class="card">
        <div class="card-image">
          <%= image_tag asset_path(pro.image) %>
          <span class="card-title"><%= pro.title %></span>
        </div>
        <div class="card-content">
          <p><%= pro.detail %></p>
        </div>
        <div class="card-action">
          <a href="<%= pro.link %>"><%= pro.link %></a>
        </div>
      </div>
      <!-- <% if @contest.end_time > Time.now %> -->
        <div class="result">
          43
        </div>
      <!-- <% else %> -->
        <div class="result">
          ??
        </div>
      <!-- <% end %> -->
    </div>
  <% end %>
</div>
  <a href="<%= new_contest_vote_path(params[:id]) %>">確認</a> 
<% end %>
<script
  src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
    integrity="sha256-k2WSCIexGzOj3Euiig+TlR8gA0EmPjuc79OEeY5L45g="
      crossorigin="anonymous"></script>
<script>
$(function(){
  $('.result-card').click(function(){
    $(this).toggleClass('active')
    
  });
});
</script>
<style>
.active{
  opacity: 0.6;
  color: #000;
}
</style>
