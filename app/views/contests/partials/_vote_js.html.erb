<script>

$('div.card-image').on('click', function(){
  console.log({id: $('input.selected_id')})
  console.log($('input.selected_id'))
  point = $('input.selected_id')[0].value
  cards = document.getElementsByClassName('card')
  for(var i = 0 ; i < cards.length ; i ++){
    $(cards[i]).css("border", "1px none #ff0000")
  }

  $('input.selected_id')[0].value = $(this).context.id
  $(this).parent().css("border", "1px solid #ff0000")
});

</script>