投票期間: <%= contest.start_time.strftime("%Y/%-m/%-d %-H:%M") %> -
<%= contest.end_time.strftime("%Y/%-m/%-d %-H:%M") %>
<% if current_user != nil && current_user.id == contest.admin_id %>
  <div style="background-color: #eee; padding: 1.2em">
    <strong>管理者機能</strong><br>
    <%= link_to "ノミネート作品の登録...", "/contests/#{@contest.id}/products/new", class: "product_register_btn" %>
  <%= render partial: "contests/partials/change_time" %>
  </div>
<% end %>
<h2>投票期間になるまでお待ちください</h2>

   
  <div class="hide-on-small-only"><!-- # for pc or tablet -->
  <%= render partial: "contests/partials/pc_products", locals: { products: products } %>
  </div>
  <div class="hide-on-med-and-up"><!-- # for smart phone -->
  <%= render partial: "contests/partials/smartphone_products", locals: { products: products } %>
  </div>
